import 'package:flutter/material.dart';

class CircleAddIcon extends StatelessWidget {
  final constraints;

  const CircleAddIcon(
    this.constraints, {
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return;
  }
}
