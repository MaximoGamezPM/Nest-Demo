import 'package:flutter/material.dart';

class CircleMinusIcon extends StatelessWidget {
  final constraints;

  const CircleMinusIcon(
    this.constraints, {
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return;
  }
}
