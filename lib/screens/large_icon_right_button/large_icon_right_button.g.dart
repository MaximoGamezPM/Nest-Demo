import 'package:flutter/material.dart';

class LargeIconRightButton extends StatelessWidget {
  final constraints;

  const LargeIconRightButton(
    this.constraints, {
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return;
  }
}
