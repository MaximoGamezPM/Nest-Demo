import 'package:flutter/material.dart';

class ShopIcon1 extends StatelessWidget {
  final constraints;

  const ShopIcon1(
    this.constraints, {
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return;
  }
}
